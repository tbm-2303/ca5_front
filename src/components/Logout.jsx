const Logout = ({onClick}) => {
    return(
        <div>
            <input type="submit" value="Submit" onClick={onClick}>
                Logout
            </input>
        </div>
    )
}
export default Logout